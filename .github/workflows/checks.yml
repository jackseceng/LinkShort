---
name: Linter

on:
  pull_request:
    branches:
      - main

permissions: read-all

jobs:
  lint:
    name: Scan
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: read
      # To report GitHub Actions status checks
      statuses: write

    steps:
      - name: Checkout
        uses: actions/checkout@de5a000abf73b6f4965bd1bcdf8f8d94a56ea815
        with:
          # super-linter needs the full git history to get the
          # list of files that changed across commits
          fetch-depth: 0

      - name: Lint Codebase
        uses: super-linter/super-linter/slim@e69f0364d2fd33c3ea08a4ca973762de2a4e655b
        env:
          DEFAULT_BRANCH: origin/main
          # To report GitHub Actions status checks
          VALIDATE_ALL_CODEBASE: true
          VALIDATE_CSS: true
          VALIDATE_CHECKOV: true
          VALIDATE_DOCKERFILE_HADOLINT: true
          VALIDATE_GITLEAKS: true
          VALIDATE_GITHUB_ACTIONS: true
          VALIDATE_HTML: true
          VALIDATE_PYTHON_BLACK: true
          VALIDATE_YAML: true
          # To find and set more language specific linters
          # Fing VALIDATE_ALL_CODEBASE on this page:
          # https://github.com/super-linter/super-linter
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}